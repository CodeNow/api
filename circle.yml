machine:
  environment:
    NODE_ENV: circle
dependencies:
  override:
    - nvm install 0.10.10
    - nvm alias default 0.10.10
    - npm install
    - make build
test:
  override:
    - NODE_ENV=circle ./node_modules/.bin/mocha --reporter spec