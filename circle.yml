machine:
  environment:
    NODE_ENV: testing
dependencies:
  override:
    - nvm install 0.10.28
    - nvm alias default 0.10.28
    - npm install
test:
  override:
    - npm run lint
    - npm run bdd
    - npm run unit
