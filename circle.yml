machine:
  environment:
    NODE_ENV: testing
dependencies:
  override:
    - nvm install 0.10.24
    - nvm alias default 0.10.24
    - npm install
test:
  override:
    - npm run lint
    - npm test
    - npm run unit
  post:
    - npm run coverage
    - npm run docs
