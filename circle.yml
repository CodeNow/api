machine:
  environment:
    NODE_ENV: circle
dependencies:
  override:
    - nvm install 0.10.12
    - nvm alias default 0.10.12
    - npm install
    - make build
test:
  override:
    - ./node_modules/.bin/mocha --reporter spec