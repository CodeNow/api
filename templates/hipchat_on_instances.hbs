{{user.login}}'s <a href="{{wrapGitHubLink headCommit.url}}">changes</a> ({{commitMessage headCommit.message}}{{{moreChangesHipchat repo commitLog}}}) to {{repo}} ({{branch}}) are deployed on servers:<br/>
{{#each instances}}
 <a href="{{../domain}}/{{this.owner.username}}/{{this.name}}">{{this.name}}</a><br/>
{{/each}}
