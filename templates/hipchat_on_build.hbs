{{user.login}}'s changes to {{repo}} ({{branch}}) are now ready.
<a href="http://{{domain}}/{{owner.login}}/boxSelection/{{repo}}/{{branch}}/{{encode headCommit.message}}/{{headCommit.id}}">Choose a server to run {{branch}}</a>.