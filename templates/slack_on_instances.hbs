{{user.login}}'s latest push to {{repo}}@{{branch}} is now runnable.
There {{commitsFormat commitLog}} in this push.
The change is deployed on
{{#each instances}}
 <http://{{domain}}/{{this.owner.username}}/{{this.name}}|{{this.name}}>
{{/each}}
Or you can click <http://{{domain}}/{{instances.[0].owner.username}}/boxSelection/{{repo}}/{{branch}}/{{encode headCommit.message}}/{{headCommit.id}}|here> to choose another Box to run {{branch}} also.