{{username}}'s latest push to {{appCode.repo}}@{{appCode.branch}} is now runnable.
There {{commitsFormat commitLog}} in this push.
The change is deployed on
{{#each instances}}
  {{this.name}}
{{/each}}