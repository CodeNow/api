{{#if instances.length}}
  {{#each instances}}
  Render instance
  {{/each}}
{{else}}
[Select Runnable server to run code from this PR](http://{{domain}}/podviaznikov/boxSelection/{{repoName}}/{{encode branch}}/commit/{{commit}})
{{/if}}